# GitHub personal access token for creating feedback issues
# Required scope: `public_repo` (for public repos) or `repo` (for private repos)
# Create one at: https://github.com/settings/tokens
GITHUB_TOKEN=
